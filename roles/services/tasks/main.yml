---
- name: Home Server docker services setup
  ansible.builtin.include_tasks: "{{ task }}"
  loop:
    - checks.yml
    - packages.yml
    - network.yml
    - env.yml
    - compose.yml
  loop_control:
    loop_var: task
