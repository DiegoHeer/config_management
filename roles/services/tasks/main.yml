---
- name: Home Server docker services setup
  ansible.builtin.include_tasks: "{{ task }}"
  loop:
    - packages.yml
    - network.yml
    - gitops.yml
    - sync.yml
    - env.yml
  loop_control:
    loop_var: task
